PORT=3000
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=spotn_fix
DB_SSL=false
JWT_SECRET=change-this-to-a-long-random-string-in-production
# Optional: cloud providers may give a single URL instead of DB_* vars
# DATABASE_URL=mysql://user:pass@host:3306/dbname
# CORS only needed when frontend and API are on different origins (local Live Server)
CORS_ORIGIN=http://127.0.0.1:5500,http://localhost:5500