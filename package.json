{
  "name": "spotnfix",
  "version": "1.0.0",
  "private": true,
  "description": "Cardinal's SpotN'Fix — demo deploy (API + frontend)",
  "scripts": {
    "postinstall": "npm install --prefix backend",
    "start": "node backend/src/server.js",
    "seed": "node backend/src/seed.js",
    "db:setup": "node backend/scripts/import-schema.js && node backend/src/seed.js",
    "generate:er-pdf": "node scripts/generate-er-pdf.js",
    "dev": "npm run dev --prefix backend"
  },
  "engines": {
    "node": ">=18"
  }
}
